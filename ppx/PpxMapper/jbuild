(jbuild_version 1)

(library
 ((name PpxMapper)
  (public_name testre-ppx.mapper)
  (libraries (compiler-libs.common unix str ocaml-migrate-parsetree))
  (preprocess (pps (ppx_tools_versioned.metaquot_406)))
  ))
